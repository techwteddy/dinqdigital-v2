# ============================================================
# SAAS STARTER KIT — ENVIRONMENT VARIABLES
# Copy this file to .env.local and fill in the values.
# NEVER commit .env.local to version control.
# ============================================================

# ─── APP ────────────────────────────────────────────────────
NEXT_PUBLIC_APP_URL=http://localhost:3000
NEXT_PUBLIC_APP_NAME="LaunchKit"
NEXT_PUBLIC_APP_TAGLINE="The smarter way to run, grow, and scale your business."
NEXT_PUBLIC_APP_DESCRIPTION="Helps modern teams automate workflows, track performance, and collaborate without friction."
NEXT_PUBLIC_PRODUCT_CATEGORY="Business operations platform"
NEXT_PUBLIC_GITHUB_REPO="https://github.com/OmarSharaf/launchkit"
NEXT_PUBLIC_DOCS_URL="https://github.com/OmarSharaf/launchkit"
NEXT_PUBLIC_SHOW_DEVELOPER_CREDIT=true
NEXT_PUBLIC_SUPPORT_EMAIL=support@launchkit.dev

# ─── DATABASE (Supabase / PostgreSQL) ───────────────────────
# Get these from your Supabase project settings > Database
DATABASE_URL="postgresql://postgres.[project-ref]:[password]@aws-0-us-east-1.pooler.supabase.com:6543/postgres?pgbouncer=true"
DIRECT_URL="postgresql://postgres.[project-ref]:[password]@aws-0-us-east-1.pooler.supabase.com:5432/postgres"

# ─── SUPABASE AUTH ───────────────────────────────────────────
# Get these from your Supabase project settings > API
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# ─── STRIPE ─────────────────────────────────────────────────
# Get these from https://dashboard.stripe.com/apikeys
STRIPE_SECRET_KEY=sk_test_...
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_test_...

# Stripe Webhook — run `stripe listen --forward-to localhost:3000/api/webhooks/stripe`
STRIPE_WEBHOOK_SECRET=whsec_...

# Stripe Price IDs (create in Stripe Dashboard → Products, then seed with db:seed)
STRIPE_PRICE_STARTER_MONTH=price_...
STRIPE_PRICE_PRO_MONTH=price_...
STRIPE_PRICE_ENTERPRISE_MONTH=price_...

# ─── EMAIL (Resend) ──────────────────────────────────────────
# Get your API key from https://resend.com
RESEND_API_KEY=re_...
EMAIL_FROM="LaunchKit <noreply@omarsharaf.me>"

# ─── OPTIONAL: ANALYTICS ────────────────────────────────────
# PostHog
NEXT_PUBLIC_POSTHOG_KEY=phc_...
NEXT_PUBLIC_POSTHOG_HOST=https://app.posthog.com

# ─── OPTIONAL: ERROR TRACKING ───────────────────────────────
# Sentry
NEXT_PUBLIC_SENTRY_DSN=https://...@sentry.io/...
SENTRY_AUTH_TOKEN=your-sentry-auth-token
